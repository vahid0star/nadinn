import { createStore } from 'vuex'

export default createStore({
  state: {
    template: `<div>{{ count }}</div>`,

    count: "user",
        
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
